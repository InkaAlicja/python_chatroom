Simple chatroom server and client(with GUI) in python3.8, using python socket(TCP). 
Variables (HOST = 'localhost'), (PORT = 50007) can be changed to setup server-client communication from different machines.
